% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{dim.pipeflow_pip}
\alias{dim.pipeflow_pip}
\title{Number of rows of a pipeflow pipeline or view}
\usage{
\method{dim}{pipeflow_pip}(x)
}
\arguments{
\item{x}{A pipeflow pipeline or view}
}
\value{
\code{nrow()} returns the number of steps as an integer; \code{ncol()}
returns the number of columns of the step table.
}
\description{
Treats a pipeline as a table of steps: \code{nrow()} returns the number of
steps, the same as \link{length.pipeflow_pip} / \code{length()}, and \code{ncol()}
returns the number of columns of the underlying step table. Views report
only the number of covered steps as rows.
}
\details{
Base R's \code{nrow()} is implemented as \code{dim(x)[1L]}, so the number
of rows and columns is provided through a \code{dim()} method for
\code{pipeflow_pip} objects.
}
\examples{
p <- pip_new() |>
  pip_add("s1", \(x = 1) x) |>
  pip_add("s2", \(x = ~s1) x + 1)
nrow(p) # 2
ncol(p) # number of columns of the step table
nrow(p) == length(p) # TRUE

v <- pip_view(p, step = "s2")
nrow(v) # 1
}
