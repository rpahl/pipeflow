% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pip_lock}
\alias{pip_lock}
\title{Lock steps against updates}
\usage{
pip_lock(x)
}
\arguments{
\item{x}{A pipeflow pip or view.}
}
\value{
The updated pipeline or view, invisibly.
}
\description{
Locks all steps of a pipeline or a subset of steps defined by a view.
Locked steps are skipped during \code{\link[=pip_run]{pip_run()}} and cannot be modified by
\code{\link[=pip_set_params]{pip_set_params()}}, \code{\link[=pip_tag]{pip_tag()}} or \code{\link[=pip_untag]{pip_untag()}}.
}
\examples{
p <- pip_new() |>
  pip_add("load", \(x = 10) x) |>
  pip_add("fit", \(x = ~load) x * 2)
pip_run(p, lgr = NULL)

# Lock only "load" via a view so it won't be re-executed or overwritten
pip_lock(pip_view(p, step = "load"))
p[["data"]][["locked"]] # TRUE, FALSE

# Locked steps are silently skipped during pip_run()
pip_run(p, lgr = NULL, force = TRUE)
p[["data"]][["out"]][[1]] # still 10 — locked, not re-executed

pip_unlock(p)
p[["data"]][["locked"]] # FALSE, FALSE
}
