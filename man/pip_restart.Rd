% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pip_restart}
\alias{pip_restart}
\title{Restart a pipeline run}
\usage{
pip_restart(x, force = TRUE, times = 1L)
}
\arguments{
\item{x}{A pipeflow pip or view.}

\item{force}{Logical indicating if all steps should be forced to run on the
restarted run. If \code{FALSE}, steps that are already in state \code{"done"} are
skipped.}

\item{times}{Maximum number of restarts to request. Once the pipeline has
been restarted \code{times} times, further calls of \code{pip_restart()} are ignored
until the next run.}
}
\value{
The updated pipeline or view, invisibly.
}
\description{
Requests a restart of the current \code{\link[=pip_run]{pip_run()}} execution. When called from
within a step function (via the \code{.self} argument), the pipeline run is
aborted and restarted from the first step. If a view was run, the view run
is restarted together with its upstream dependencies.
}
\examples{
p <- pip_new("restart") |>
  pip_add("load", \(n = 3) seq_len(n)) |>
  pip_add("model", \(x = ~load) {
    if (length(x) == 3L) {
      pip_restart(.self)
    }
    x * 2
  })

pip_run(p)
p
}
\seealso{
\code{vignette("v06-self-modify-pipeline", package = "pipeflow")}
for an advanced example of dynamic pipelines.
}
