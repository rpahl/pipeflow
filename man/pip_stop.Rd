% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pip_stop}
\alias{pip_stop}
\title{Stop a pipeline run}
\usage{
pip_stop(x)
}
\arguments{
\item{x}{A pipeflow pip or view.}
}
\value{
The updated pipeline or view, invisibly.
}
\description{
Aborts the current \code{\link[=pip_run]{pip_run()}} execution. When called from within a step
function (via the \code{.self} argument), the pipeline run is stopped after the
current step. Steps that were not executed are marked as \code{"outdated"}.
}
\examples{
p <- pip_new("stop") |>
  pip_add("load", \(n = 3) seq_len(n)) |>
  pip_add("model", \(x = ~load) {
    if (length(x) == 3L) {
      pip_stop(.self)
    }
    x * 2
  }) |>
  pip_add("report", \(x = ~model) paste("result:", x))

pip_run(p)
p
}
\seealso{
\code{vignette("v06-self-modify-pipeline", package = "pipeflow")}
for an advanced example of dynamic pipelines.
}
